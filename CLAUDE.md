# NixOS Global Skill — Terrya's Configuration

This file serves as a comprehensive skill/guide for working with this NixOS flake-based configuration. It documents all modules, patterns, conventions, and available commands.

---

## Build & Apply

```bash
# Full system rebuild (system + home-manager in one shot)
sudo nixos-rebuild switch --flake ~/nixos-config#nixos && sudo systemctl daemon-reload
alias: nixrebuild

# Home-manager only (standalone, for testing user changes without root)
home-manager switch --flake .#terrya
alias: homerun

# Update flake lockfile
nix flake update

# Garbage collection
sudo nix-collect-garbage -d
```

---

## Architecture Overview

This is a **flake-based NixOS 26.05** config for a single machine (`nixos`). Home-manager is integrated at the system level (not standalone), so `nixos-rebuild switch` applies both system and user config atomically.

### Entry Points

| File | Purpose |
|---|---|
| `flake.nix` | All inputs; single `nixosConfigurations.nixos` output; passes `inputs` to all modules |
| `configuration.nix` | System-level: boot, networking, users, packages, display manager, services, fonts, nix settings |
| `home.nix` | User-level: shell (Zsh + oh-my-zsh), Neovim (LazyVim), home packages, shell aliases, tailscale |
| `hardware-configuration.nix` | Auto-generated by `nixos-generate-config` — do not edit |

### Secret / Aux Entries

| Entry | Purpose |
|---|---|
| `hypr/` | Legacy raw Hyprland config files (autostart, scripts, wallpapers) — NOT managed by home-manager |
| `mynixvim/` | Standalone flake for custom Neovim distribution |
| `nixvim/` | External nixvim flake (git submodule) — `nix run ~/nixos-config/nixvim#` |
| `nixpkgs.sh` | Shell script bundled as `ns` system package (for quick nixpkgs navigation) |
| `nixos-config.code-workspace` | VS Code workspace file |

---

## Module Split: `system/` vs `user/`

### `system/` — NixOS-level modules (imported by `configuration.nix`)

| Module | What it does |
|---|---|
| `system/services.nix` | Cockpit, duplicati, tailscale, NFS server, OpenSSH SFTP |
| `system/virt.nix` | Docker, Podman, libvirtd/QEMU/KVM, distrobox |
| `system/containers/pegaprox.nix` | NixOS container config for pegaprox |
| `system/WM/hyprland.nix` | Enables Hyprland WM, Waybar, dunst, hardware graphics |
| `system/WM/qtile.nix` | Enables Qtile X11 WM |
| `system/WM/dms.nix` | DMS-Shell (quickshell-based): system monitoring, theming, audio viz, clipboard, VPN |
| `system/WM/xfce.nix` | XFCE desktop environment (unused presently) |

### `user/` — Home-manager modules (imported by `home.nix`)

| Module | What it does |
|---|---|
| `user/WM/hyprland.nix` | Hyprland keybinds, window rules, animations, monitor config, GTK theme |
| `user/WM/mangowm.nix` | Mango WM config (settings via `wayland.windowManager.mango`): effects, animations, keybinds, window rules, tag rules, dms IPC binds, autostart |
| `user/programs.nix` | direnv, eza, claude-code, opencode, television, tmux plugins, AWS CLI |
| `user/editors/zed-editor.nix` | Zed editor configuration |
| `user/editors/lsp-linters.nix` | LSP/linter configs for various languages |
| `user/shells/sh.nix` | POSIX shell config |
| `user/terminals/kitty.nix` | Kitty terminal config |

---

## Flake Inputs

| Input | Purpose | Used In |
|---|---|---|
| `nixos-unstable` | NixOS packages channel | Everything |
| `home-manager` (master) | User env management | `flake.nix`, `home.nix` |
| `quickshell` | Qt-based shell components | `system/WM/dms.nix` |
| `dms-plugin-registry` | DMS-Shell plugins | `system/WM/dms.nix` |
| `lazyvim` | LazyVim Neovim framework | `home.nix` (via `lazyvimModule`) |
| `mangowc` | Mango WM Wayland compositor (dwl-based, scenic animations, tags) | `flake.nix`, `configuration.nix`, `user/WM/mangowm.nix` |
| `nur` | Nix User Repository | `flake.nix`, `configuration.nix` |
| `nix-software-center` | GUI package manager | `configuration.nix` |
| `nixvim` | Custom nixvim Neovim build | `configuration.nix` (system package) |

---

## Conventions

### Module Structure
- **System modules** (`system/`) receive `{ pkgs, ... }` or `{ config, pkgs, inputs, ... }`
- **User modules** (`user/`) receive `{ config, lib, pkgs, inputs, ... }`
- All modules get `inputs` via `specialArgs` in `flake.nix`

### Adding a new system-level feature
1. Create module in `system/<category>/<name>.nix`
2. Import it in `configuration.nix` under `imports`
3. If it needs a flake input, add to `flake.nix` and pass via `specialArgs`

### Adding a new user program
1. Create module in `user/<category>/<name>.nix`
2. Import in `home.nix` under `imports`
3. If it needs `lazyvimModule`, pass it as extra arg

### General Rules
- `hardware-configuration.nix` — never edit manually
- New categories go in `system/` or `user/` subdirectories
- Use `environment.systemPackages` for system-wide packages (in `configuration.nix`)
- Use `home.packages` for user-specific packages (in `home.nix`)
- Secrets use `/etc/secrets/` path (see crypttab entry for LUKS key)
- `allowUnfree = true` is set globally; extra unfree pkgs can be added to `allowUnfreePredicate`

---

## Important Config Details

### Users
- **Primary**: `terrya` — in groups: networkmanager, wheel, disk, flatpak, qemu, kvm, libvirtd, docker, sshd, audio, video
- **Shell**: Zsh with oh-my-zsh (af-magic theme)
- **Groups**: 10+ groups for devops/virtualization workflows

### Desktop Environments Active
- **COSMIC** (primary, systemd-boot entry)
- **GNOME** (secondary)
- Hyprland, Qtile, DMS-Shell, Mango WM (available via display manager selection)

### Key Aliases (in `home.nix`)
| Alias | Command |
|---|---|
| `nixrebuild` | `sudo nixos-rebuild switch --flake ~/nixos-config#nixos && sudo systemctl daemon-reload` |
| `homerun` | `home-manager switch --flake .#terrya` |
| `vl` | `NVIM_APPNAME=lazyvim nvim` (LazyVim variant) |
| `neovim` | `nix run ~/neovim-flake#` (standalone flake) |
| `dbeb` | `distrobox enter ubuntu` |

### LUKS/BTRFS
- Secondary drive decrypted via `/etc/crypttab` with key at `/etc/secrets/riot.key`
- Mounted at `/mnt/riot` as btrfs

### Virtualization Stack
- Docker + Podman (with DNS) + libvirtd/QEMU (with swtpm) + distrobox

---

### Mango WM Integration
- **System-level**: `programs.mango.enable = true` is in `configuration.nix:250`, provided by `mangowc.nixosModules.mango` from `flake.nix`
- **User-level**: `wayland.windowManager.mango.settings` in `user/WM/mangowm.nix` generates `~/.config/mango/config.conf`
- **Session wrapper**: `start-mango` in `configuration.nix:16-33` wraps session launch with env setup (Wayland env, env import); custom `.desktop` at `configuration.nix:34-46` overrides the bare `Exec=mango` from upstream
- **DMS as bar**: DMS-Shell (`system/WM/dms.nix`) provides the panel — Waybar is NOT started in autostart to avoid double bars
- **DMS scripts**: Audio/notifications/night-light helpers in `~/.dotfiles/mangowc/.config/mango/scripts/`
- **Autostart**: `exec-once` managed by home-manager module in `user/WM/mangowm.nix:15-26`; generates `~/.config/mango/autostart.sh`
- **Rebuild**: `nixrebuild` applies both system and user config atomically
- **Selecting Mango**: Available in display manager session list after rebuild

## Quick Reference — Common Changes

**Add a system package:**
→ Edit `environment.systemPackages` in `configuration.nix:177-246`

**Add a user package:**
→ Edit `home.packages` in `home.nix:106-146`

**Modify Hyprland keybinds:**
→ Edit `bind` array in `user/WM/hyprland.nix:119-175`

**Modify Mango WM keybinds/settings:**
→ Edit `user/WM/mangowm.nix` settings (effects, binds, rules, tags, colors)
→ DMS bar config: `~/.dotfiles/mangowc/.config/mango/config.jsonc` + `style.css` (stow-managed)

**Modify mango session wrapper:**
→ Edit `start-mango` script in `configuration.nix:16-33`

**Change shell aliases:**
→ Edit `home.shellAliases` in `home.nix:88-103`

**Add a flake input:**
→ Add to `inputs` in `flake.nix:4-29`, then pass in `specialArgs` and/or `nixosSystem` call

**Add a flake input:**
→ Add to `inputs` in `flake.nix:4-29`, then pass in `specialArgs` and/or `nixosSystem` call
